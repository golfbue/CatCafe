# คู่มือการใช้งาน CatCafe

## 1. ภาพรวมโปรเจกต์
โปรเจกต์นี้ประกอบด้วย:
- `backend/` — เซิร์ฟเวอร์ Node.js (Express) และ API สำหรับจัดการข้อมูล
- `frontend/` — หน้าเว็บ HTML/CSS/JS สำหรับใช้งานระบบ
- `run_guide.md` — คู่มือนี้สำหรับเพื่อนของคุณ

## 2. เตรียมก่อนรัน
### 2.1 เครื่องที่ต้องติดตั้ง
- Node.js เวอร์ชันล่าสุดหรือ v18+ (รวม `npm` ด้วย)
- หากใช้ Neo4j หรือฐานข้อมูลอื่นเพิ่มเติม ให้อ่านโค้ด backend ก่อน

### 2.2 ตรวจสอบไฟล์สำคัญ
- `backend/package.json` — สคริปต์รัน backend
- `backend/server.js` — เซิร์ฟเวอร์หลัก
- `frontend/index.html` — หน้าเว็บหลัก

## 3. ขั้นตอนการรันระบบ
### 3.1 เปิด VS Code และเข้าโฟลเดอร์โปรเจกต์
เปิด VS Code ที่โฟลเดอร์ `CatCafe` แล้วเปิด Terminal ใน VS Code

### 3.2 เข้าไปที่โฟลเดอร์ backend
รันคำสั่ง:
```powershell
cd "c:\Users\golfk\OneDrive\เอกสาร\GitHub\CatCafe\backend"
```

### 3.3 ติดตั้ง dependencies
รันคำสั่ง:
```powershell
npm install
```

### 3.4 เริ่มเซิร์ฟเวอร์
รันคำสั่ง:
```powershell
npm start
```

ถ้าทุกอย่างถูกต้อง จะเห็นข้อความว่า:
```
Server is running on http://localhost:5000
```

## 4. เปิดใช้งานหน้าเว็บ
เปิดเบราว์เซอร์แล้วไปที่:
```
http://localhost:5000
```

## 5. การใช้งานระบบ
### 5.1 หน้าแรก
- หน้าหลักจะแสดงเมนูและปุ่มสำหรับเริ่มใช้งาน
- หากยังไม่ได้เข้าสู่ระบบ ให้สมัครสมาชิกหรือเข้าสู่ระบบก่อน

### 5.2 สั่งอาหาร
- เมื่อกดสั่งอาหาร ระบบจะสร้างการเข้าใช้งานให้อัตโนมัติ
- เลือกเมนูแล้วเพิ่มลงตะกร้า จากนั้นกดสั่งอาหาร

### 5.3 ยืมบอร์ดเกม
- หากยังไม่มีการเข้าใช้งาน ระบบจะทำการเช็คอินให้ก่อน
- กดปุ่มยืมเกมเพื่อบันทึกสถานะเกมเป็นยืมแล้ว

### 5.4 ชำระเงิน
- หน้า `ชำระเงิน` จะแสดงสรุปค่าบริการและรายการอาหารที่สั่ง
- กดชำระเงินเพื่อสิ้นสุดการเข้าใช้บริการ

## 6. บัญชีตัวอย่างสำหรับทดสอบ
ระบบ seed ข้อมูลตัวอย่างไว้แล้ว หากรัน endpoint seed จะได้ข้อมูลเริ่มต้น
- ลูกค้า: `John Doe` / password: `password123`
- พนักงาน: `sirapop` / password: `admin`

ถ้าต้องการสร้างข้อมูลเอง ให้ใช้ระบบสมัครสมาชิกบนหน้าเว็บ

## 7. ปัญหาที่พบบ่อย
- ถ้า `npm install` ขึ้น error ให้แน่ใจว่าอยู่ในโฟลเดอร์ `backend`
- ถ้า `npm start` ขึ้น error ให้ตรวจสอบไฟล์ `backend/package.json`
- หากหน้าเว็บไม่โหลด ให้ตรวจสอบว่า backend รันอยู่ที่พอร์ต `5000`

## 8. คำแนะนำสำหรับเพื่อน
- รัน `npm install` แค่ครั้งแรกเท่านั้น
- ถ้าปิด terminal แล้วเปิดใหม่ ให้รัน `cd backend` ก่อน
- เวลาทดสอบ frontend ให้ใช้ `http://localhost:5000`

---
เขียนโดยผู้พัฒนา CatCafe เพื่อให้เพื่อนของคุณเริ่มใช้งานได้ทันที